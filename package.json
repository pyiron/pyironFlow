{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.jsx --minify --format=esm --bundle --outdir=src/pyiron-xyflow/static"
	},
	"dependencies": {
                "@xyflow/react": "^12.0.4",
		"@anywidget/react": "^0.0.7",
		"react": "^18.3.1",
		"react-dom": "^18.3.1"
	},
	"devDependencies": {
		"esbuild": "^0.23.0"
	}
}
